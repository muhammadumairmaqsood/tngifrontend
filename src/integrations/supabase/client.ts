// Supabase client is currently unused because all content
// is served from a static configuration. This file is kept
// for future use if you decide to re-enable Supabase.
//
// To re-enable, restore the client creation code and update
// the hooks in `src/hooks/useSiteContent.ts` to call Supabase.

export {};